# ETF 轮动项目规划

更新日期：2026-07-03

## 已确认决策

- 项目路径：`/Users/sweethome/Qoder/etf-rotation`
- 存储方式：本地 Parquet 文件
- 研究区间：`2021-01-01` 至 `2026-07-03`
- ETF 池：原方案中的行业 ETF 与风格 ETF 池
- 基准：`510300.SH` 沪深300ETF
- 执行要求：每个阶段都要保留记录文档和可运行测试

## 阶段路线

### 第一阶段：数据基础设施

目标：建立可复现的本地数据层。

交付内容：

- ETF 池与项目配置。
- 合成数据、iFinD MCP CLI、iFinD HTTP API 的数据源抽象。
- Parquet 存储工具。
- ETF 日线数据质量检查。
- 数据采集与校验 CLI 命令。
- 阶段记录与测试。

状态：已完成。项目已接入合成数据和 iFinD HTTP 数据，并通过 `cmd_history_quotation` 采集真实 ETF 历史行情。

### 第二阶段：因子计算与回测

目标：实现并验证 ETF 轮动信号框架。

交付内容：

- 动量、估值、景气、资金流、拥挤度、宏观共振因子。
- 因子标准化与综合打分。
- 单因子 IC 测试。
- 每周调仓回测引擎。
- 基线策略报告。

状态：已完成可运行基线。当前真实数据版本已实现动量、资金流、拥挤度和每周 Top-N 轮动。宏观 EDB 数据和大部分行业指数日线已采集；第一版宏观共振排序因子验证失败，不纳入主策略。估值和景气因子仍待推进，因为当前行业估值/一致预期查询返回空表。

### 第三阶段：组合优化与风险控制

目标：把信号转化为更可执行的组合。

交付内容：

- 风险平价或逆波动加权。
- 持仓约束。
- 止损、回撤、波动率和拥挤度控制。
- 参数敏感性分析。

状态：进行中。动量 Top3 + 得分门槛候选已完成滚动样本外、交易成本、调仓频率和流动性门槛验证。宏观风险 overlay 已实现并评估，但因全样本最大回撤没有改善，暂作为可选防守分支。

仓位分配方案已完成并排比较。当前 `Top3 + 单只25%上限 + 现金` 是稳健主分支；`Top3 + 单只33.33%上限` 是进攻备选分支；`Top4 + 单只25%上限` 因回撤和 Sharpe 表现较弱，暂不提升为主策略。详见 `ALLOCATION_VARIANT_RECORD.md`。

轻量机器学习模型已完成初步滚动样本外验证。Ridge、Logistic 和带少量交互项的 Ridge 均未打赢当前线性动量规则，因此暂不替代主策略。详见 `ML_MODEL_EVALUATION_RECORD.md`。

ML 状态识别仓位 overlay 已完成第一版验证。该方法不直接选 ETF，只判断当前 Top3 动量组合该用 75% 仓位还是满仓。阈值 `0.50` 的样本外平均收益和 Sharpe 均高于固定 75% 仓位，回撤接近固定 75% 仓位，属于当前最值得继续跟踪的增强分支。详见 `ML_REGIME_OVERLAY_RECORD.md`。

### 第四阶段：模拟跟踪与迭代

目标：实盘前先运行信号跟踪和模拟组合。

交付内容：

- 每日/每周信号刷新。
- 模拟组合台账。
- 回测假设与实时数据的偏差检查。
- 早期实盘前的人工确认流程。

## 当前主策略与最新信号

当前主策略：`m_1_3_6_top3_min0p6`。

- 每周五调仓。
- 选择动量得分最高且分数不低于 `0.60` 的 3 只 ETF。
- 单只 ETF 上限 `25%`，剩余资金留在现金。
- 基准为 `510300.SH` 沪深300ETF。

最新数据日期：`2026-07-03`。最近一次组合调仓日：`2026-07-03`。

| 资产 | 名称 | 权重 |
|---|---|---:|
| `159995.SZ` | 芯片ETF | 25% |
| `515000.SH` | 科技ETF | 25% |
| `159915.SZ` | 创业板ETF | 25% |
| `CASH` | 现金 | 25% |

最新宏观 overlay 信号日期：`2026-07-03`。宏观风险分数为 `0.3620`，目标仓位为 `100%`，当前不触发额外降仓。

## 下一步决策队列

- 继续以每周调仓作为当前主分支。
- 把 20 日平均成交额 `5000万` 作为模拟跟踪前的第一层执行约束候选。
- 将防守分支 `m_3_6_trend_top3_min0p6` 接入同一套验证框架并行观察。
- 保留宏观 overlay 为可选防守分支，并加入市场趋势/市场宽度确认，减少误降仓。
- 并行监控 `Top3 满仓集中` 进攻分支，但主策略暂不从 75% 权益仓位上限切换到满仓。
- 暂不使用 ML 模型替代 ETF 排名；后续若继续研究，优先把 ML 放到市场状态识别和仓位控制层。
- 重点跟踪 `ML regime overlay`，并尝试加入市场宽度、宏观风险和成交额趋势特征，验证能否稳定优于固定 75% 与固定满仓。
- ETF 池可以扩大，但需作为新策略版本处理。用户已确认投资范围不包括跨境、黄金、债券 ETF，因此只测试 A 股股票 ETF 核心扩展池，补齐科创、银行、白酒、新能源车、光伏、AI、传媒、化工、电力、农业/养殖等暴露。详见 `ETF_POOL_EXPANSION_RECORD.md` 和 `EXPANDED_POOL_BACKTEST_RECORD.md`。
- 解决行业估值/一致预期字段的标识符问题：使用精确 iFinD 行业代码、成分股聚合，或阶段性推迟。
- 复核地产和科技行业指数代理；当前返回序列较稀疏。
